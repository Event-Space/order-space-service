spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:space_service}
    username: ${DB_USERNAME:k_app}
    password: ${DB_PASSWORD:123}
  jpa:
    hibernate:
      ddl-auto: update



application:
  title: Project For SWE
  version: 1.0.0

springdoc:
  api-docs:
    enabled: true
    path: /order-api
  swagger-ui:
    path: /swagger







